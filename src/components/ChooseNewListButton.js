import React from "react";

function ChooseNewListButton(props) {
  return (
    <div>
      <h3>game over</h3>
      <button onClick={props.onClick}>Choose new list</button>
    </div>
  );
}

export default ChooseNewListButton;
